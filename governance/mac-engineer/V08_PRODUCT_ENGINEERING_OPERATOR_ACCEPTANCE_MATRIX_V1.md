# ENGÜRÜ Mac Engineering™ v0.8 — Product Engineering Operator Acceptance Matrix v1

## STATE

**CANONICAL / ACTIVE**

## CURRENT TRUTH

v0.7 Long-Running Reliability is **VERIFIED / LOCKED** with 13/13 closure, Human Threshold™ ACCEPTED and ENGÜRÜ Mac-Native Engineering Authority™ verified as the primary local engineering authority.

v0.8 begins from that locked truth. It does not reopen v0.7.

Product source baseline:

- repository: `engurulabory/enguru-mac-engineer`
- exact main at v0.8 start: `5432b9b135499cea18273c0e003877b864af92c6`
- installed bundle observed before v0.8 field audit: `0.6`
- final target remains: **v1.2 — Local Mac Astra Verified Final**

## AUTHORITY MODEL

Primary local engineering authority:

> **ENGÜRÜ Mac-Native Engineering Authority™ — Mac-local Build, Repair, Recovery & Verification Authority.**

Remote identity / synchronization / publication / recovery / external Evidence surface:

> **GitHub**

Machine verification authority:

> **DoneCheck™ v1.2 — version 1.2.0 — exact main `8b90a8fc93453dd8a84994195d28d14b15e261cb`**

Final human authority:

> **Human Threshold™**

## PRODUCT ENGINEERING CHAIN

`INTAKE → DISCOVER → ARCHITECT → DESIGN → BUILD → TEST → REPAIR → PACKAGE → DEPLOY → LIVE VERIFY → LIFECYCLE → EVIDENCE → DoneCheck™ v1.2 → Human Threshold™`

## ACCEPTANCE RULE

A gate is PASS only when its stated Evidence exists and the real field behavior matches the criterion.

A claim, checklist edit, version number, UI label or static PASS string cannot create PASS.

Unknown or contradictory state resolves to **HOLD / ATTENTION_REQUIRED / RECOVERY_REQUIRED** according to observed truth.

## 12-GATE v0.8 CLOSURE CONTRACT

### Gate 1 — Canonical Transition + Self-Engineering Baseline

Acceptance:

- v0.7 exact-head Verified Finish remains preserved;
- v0.8 control-plane branch begins from the sealed v0.7 head;
- active-version canonical boot selects `currentV08`;
- OSi operator reads v0.8 current truth;
- ENGÜRÜ Mac Engineer product exact-main is clean and verified;
- runtime compile + full regression PASS;
- native prep syntax + Swift build PASS;
- installed/source bundle version and runtime parity are measured;
- current UI form is measured;
- Evidence is produced with zero product mutation.

Exit:

`V08_SELF_ENGINEERING_BASELINE_AUDIT_PASS`

### Gate 2 — Product Reality Reconciliation

Acceptance:

- baseline Evidence is consumed;
- source/runtime/installed-app provenance differences are explicit;
- current native-shell vs web-runtime boundary is explicit;
- current bundle/version truth is explicit;
- stale governance or stale product metadata is reconciled;
- required difference list is bounded and ordered;
- no feature work starts from an unmeasured assumption.

Exit:

`V08_PRODUCT_REALITY_RECONCILED`

### Gate 3 — UX + Aesthetic Product Contract

Acceptance:

- final user questions and primary workflow are explicit;
- UI surface is simplified from measured current reality;
- Aesthetic Motor is reused through the existing architecture;
- overflow/originality/accessibility/responsive gates are defined;
- Human Artistic Authority™ review point is explicit;
- desktop Mac primary experience is field-testable.

Exit:

`V08_UX_AESTHETIC_CONTRACT_VERIFIED`

### Gate 4 — Full Product Engineering Chain Binding

Acceptance:

- INTAKE, DISCOVER, ARCHITECT, DESIGN, BUILD, TEST, REPAIR, PACKAGE, DEPLOY, LIVE VERIFY, LIFECYCLE, EVIDENCE and DoneCheck™ v1.2 are bound to one governed task lifecycle;
- each transition has explicit state/evidence authority;
- existing reliability/recovery/idempotency/single-writer layers are reused;
- unknown transitions fail closed;
- no parallel verification authority is created.

Exit:

`V08_FULL_CHAIN_BOUND`

### Gate 5 — Native App Productization + Version/Provenance

Acceptance:

- source/runtime/app bundle provenance is one coherent chain;
- native app build, install, launch and readiness are verified;
- bundle version reflects the achieved product milestone only after Evidence;
- update path preserves rollback;
- installed runtime/source parity is explicit;
- application remains Mac-local by default.

Exit:

`V08_NATIVE_PRODUCTIZATION_VERIFIED`

### Gate 6 — Existing Product Change Scenario

Acceptance:

- ENGÜRÜ Mac Engineer performs a real bounded change to an existing product;
- discovery → architecture → build → test → repair where needed → package → verification is exercised;
- regression is clean;
- Evidence is complete;
- resulting delivery is usable by the human operator.

Exit:

`V08_EXISTING_PRODUCT_CHANGE_VERIFIED`

### Gate 7 — Migration / Repair Scenario

Acceptance:

- a real broken or migration-required product state is observed;
- root cause is evidenced;
- minimal repair/migration is applied;
- recovery/rollback basis exists;
- regression + restart/persistence where relevant are verified;
- finished result is delivered with Evidence.

Exit:

`V08_MIGRATION_REPAIR_VERIFIED`

### Gate 8 — New Product from Brief Scenario

Acceptance:

- a bounded new-product brief is accepted;
- required discovery/research is completed;
- architecture/design decisions are evidenced;
- product is built, tested, packaged and made runnable;
- deployment/live verification is applied when the product type requires it;
- finished-product delivery is human-usable.

Exit:

`V08_NEW_PRODUCT_FROM_BRIEF_VERIFIED`

### Gate 9 — Deploy / Live Verify / Rollback + Lifecycle

Acceptance:

- deployable product path performs controlled release;
- live truth is verified independently of local build truth;
- rollback is rehearsed or verified through the applicable product lifecycle;
- update / stop / restart / remove or equivalent lifecycle controls are verified;
- Evidence continuity survives lifecycle operations.

Exit:

`V08_RELEASE_LIFECYCLE_VERIFIED`

### Gate 10 — Finished-Product Delivery Acceptance

Acceptance:

- completion means usable finished product, not code completion;
- user-facing onboarding/launch path is clear;
- operational status is understandable;
- Evidence bundle is complete;
- unresolved technical items are explicit;
- Human Threshold receives a concise decision surface.

Exit:

`V08_FINISHED_PRODUCT_DELIVERY_ACCEPTED`

### Gate 11 — Consolidated Real-Mac Product Engineering Commissioning

Acceptance:

- the complete v0.8 chain is exercised on real Mac;
- self-engineering of ENGÜRÜ Mac Engineer is included;
- at least the required scenario classes above are represented by real Evidence;
- restart/recovery and continuity remain intact;
- no second canonical truth is created;
- technical HOLD target is zero except explicitly external/deferred surfaces.

Exit:

`V08_CONSOLIDATED_MAC_COMMISSIONING_PASS`

### Gate 12 — DoneCheck™ v1.2 + Human Threshold™ + Version Lock

Acceptance:

- DoneCheck™ v1.2 exact-main authority consumes criterion-scoped Evidence from Gates 1–11;
- machine result is PASS;
- external/deferred evidence remains accurately classified;
- Human Threshold™ explicitly accepts;
- canonical state/worklist/roadmap reconcile;
- v0.8 is locked only after the final acceptance receipt.

Exit:

`PRODUCT_ENGINEERING_OPERATOR_VERIFIED_LOCKED`

## CURRENT ACTIVE GATE

**Gate 9 — Deploy / Live Verify / Rollback + Lifecycle**

Gate 8 exit:

`V08_NEW_PRODUCT_FROM_BRIEF_VERIFIED`

Gate 8 state: **VERIFIED PASS / SEALED**.

## NEXT ACTION

`V08_RELEASE_LIFECYCLE_SCENARIO → controlled release → independent live verification → rollback → lifecycle controls → Evidence continuity → V08_RELEASE_LIFECYCLE_VERIFIED`
